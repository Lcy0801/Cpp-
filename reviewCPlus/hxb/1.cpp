#include <iostream>
#include <vector>
using namespace std;

vector<vector<int>> demo(vector<vector<int>> points)
{
    vector<int> p1 = points[0];
    vector<int> p2 = points[0];
    vector<int> p3 = points[0];
    vector<int> p4 = points[0];

}

int main()
{
    vector<int> s;
    s.push_back(1);
    auto v = s;
    cout << v[0] << endl;
    system("pause");
    return 0;
}